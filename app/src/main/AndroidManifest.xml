<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home"></activity>
        <activity android:name=".Listening" />
        <activity android:name=".Match" />
        <activity android:name=".Spelling" />
        <activity android:name=".Play" />
        <activity android:name=".SingleNumCol" />
        <activity android:name=".SingleAnimal" />
        <activity android:name=".LearnColors" />
        <activity android:name=".SingleLetter" />
        <activity android:name=".LearnLetNumAni" />
        <activity android:name=".LearnPlay" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>